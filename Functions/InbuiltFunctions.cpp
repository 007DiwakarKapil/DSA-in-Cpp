#include <bits/stdc++.h>  //header file that includes everything
using namespace std;
int main(){
    cout<<max(51,72);               // int max(int a,int b){
                                    // if(a>b) return a;
                                    // else return b;
                                    // }
}
// min(a,b)  minimum value (we cannot put more than 2 numbers in min,max.To do so we have to build our own function.)
// pow(a,b) gives a^b.
// sqrt(a) gives square root of a.
// cbrt(a) gives cube root of a.
// abs(a) gives absolute value of a.