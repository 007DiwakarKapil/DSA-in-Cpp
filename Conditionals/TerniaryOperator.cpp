// condition ? true : false  (Bad ass if-else)
#include <iostream>
using namespace std;
int main(){
    cout<<"Enter a number : ";
    int n;
    cin>>n;
    n%2==0 ? cout<<"Even" : cout<<"odd";
}